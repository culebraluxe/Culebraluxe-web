# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/

# Environment variables
.env*.local

# Local pnpm store (workspace-local store used by the sandboxed toolchain)
.pnpm-store/

# Agent worker scheduler local overrides (optional, machine-specific)
.env.scheduler

# Common ignores
node_modules
.next/
.DS_Store
.neon
.aider*

# Apple Contacts — private export data (never commit)
contact-export/contacts-export.json

# Apple Messages — local exporter build artifacts + private export data (never commit)
apple-messages-export/.build/
apple-messages-output/
apple-messages-export/output/
messages_schema_recon.txt
