# ---------------------------------------------------------------------------
# Gitleaks baseline — KNOWN historical exposure, not an allowance.
#
# Established 2026-09-18 by the first gitleaks run in this repository. Every fingerprint below is a
# finding that already exists in git history and CANNOT be removed by editing today's tree:
#   * 90 findings in `.env.local.before-icloud-username-fix`, committed across 9 commits on
#     2026-09-03/04 — a backup of the environment file, carrying 48 variable names including
#     DATABASE_URL_PROD, AUTH_SECRET, WHATSAPP_ACCESS_TOKEN, BOLDSIGN_API_KEY and
#     ICLOUD_MAIL_APP_PASSWORD. The file is gone from HEAD; the history is not.
#   * 3 findings in internal API routes (jessica-listing-v4, signature-provision,
#     signature-provision-reset), all since deleted from the tree.
#
# A baseline is honest only while it is a RECEIPT. Rotation of the affected credentials is the
# actual remedy and is tracked by ENG-FORGE-SECRET-HISTORY-01; when the credentials are rotated
# this file stays as the record, and the exposure stops being live.
#
# New findings are NOT added here to make a scan pass. Every addition needs a line saying why.
# ---------------------------------------------------------------------------

05798924a3cf0e8ee452235779470671f08e908f:app/api/internal/signature-provision-reset/route.ts:generic-api-key:7
14298904e4b7718747b42da3276c15689e7c5ab7:app/api/internal/signature-provision/route.ts:generic-api-key:8
847b7a27d87a5ba2b3e42ed24449a3f4bc0796d7:app/api/internal/jessica-listing-v4/route.ts:generic-api-key:5
