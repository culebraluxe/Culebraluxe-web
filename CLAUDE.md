# Adapter — not the handbook

Read `@AGENTS.md`. Then `docs/agent/ORIENTATION.md`, `docs/agent/packets/<story-id>.md`, and `docs/agent/MEMORY.md`.

The generated block below is the machine-checked half of this pointer: `pnpm forge:sync-agents` writes it, and `pnpm forge:packet-lint` fails when it drifts. Everything else belongs in `AGENTS.md`, not here. When Claude Code reads AGENTS.md natively, delete this file.

<!-- FORGE:HANDBOOK:BEGIN -->

Adapter block — GENERATED by `pnpm forge:sync-agents`. Do not hand-edit.

This file is a pointer, not a second copy of the rules. Read the handbook:
- `AGENTS.md`
- `docs/agent/ORIENTATION.md`
- `docs/agent/packets/<STORY-ID>.md`
- `docs/agent/MEMORY.md`

Load-bearing rules (source `AGENTS.md`; the lint fails if a source sentence disappears):
- Only the Builder role commits. Scout, Assay and Inspector never do.
- Route every failure through the durable capture framework; a swallowed catch is a defect.
- Never special-case one listing (Casa Luar or any other) in application code.
- Never commit secrets or `.env.local`.

Commands:
- `pnpm forge:manifest <STORY-ID>` — the ranked files to read for a story
- `pnpm forge:packet-lint` — the harness gate (packets, skills, maps, this block)
- `pnpm test:harness` — the harness tests
- `pnpm forge:batch:status` — read-only engine and batch state

Regenerate with `pnpm forge:sync-agents`; check with `pnpm forge:sync-agents --check`.

<!-- FORGE:HANDBOOK:END -->
