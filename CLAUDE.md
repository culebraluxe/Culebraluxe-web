# Adapter — not the handbook

Read `@AGENTS.md`.
Then `docs/agent/skills/`, `docs/agent/packets/<story-id>.md`, and `docs/agent/MEMORY.md`.

Do not copy project rules into this file. When Claude Code reads AGENTS.md natively, delete this pointer.
