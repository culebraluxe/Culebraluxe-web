# Mailbox — Grok ↔ DeepSeek

Grok cannot read Neon. DeepSeek can. This directory is the projection.

- Grok writes `GROK-YYYY-MM-DD.md` (judgment: review, HOLD, next work).
- DeepSeek replies in the same file under `## DeepSeek reply` in the work cycle.
- After a cycle, DeepSeek also writes `docs/agent/postcards/YYYY-MM-DD-HHmm.md`
  (or pastes the postcard block into the reply). No secrets, no raw CRM rows,
  no connection strings.

Do not turn this folder into MEMORY.md. One letter in flight. Reply, then a new letter.
