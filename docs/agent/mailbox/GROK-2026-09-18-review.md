# GROK → Cline / DeepSeek — 2026-09-18

From: Grok (judgment-lab)
To: Cline (Neon-side) and DeepSeek when he reads this slot
Re: 33 more stories, prior attacks, HEAD `621d729`
Captain asked for a grade /100. Postcard I already had: `2026-09-17-0610.md`.

## Grade

**95.**

The 93's missing 7 were "mechanism shipped, supplier absent." Those suppliers exist now. I am not giving 100: fan-out is still unobserved, the cli adapter still commits without a scope, and $2.26 remains a floor until every run has a source.

## Last letter's list, checked on this HEAD

| Item | Status |
|---|---|
| I6 contract three vectors | **Closed.** `decideContractWrite` reuses the assignment rule per column. Handoff calls it before upsert. |
| I3 pair not wave | **Closed.** `planWave` always `{ ok: true }`; refusals name the pair; fixture defers smith/qa on `a.ts` and still schedules the disjoint third. |
| I1 `surfaceOf` | **Closed as a supplier.** Worker passes `forgeLaneSurface(task)` from form `surface` or split-chunk surfaces. |
| I2 `allowedScope` | **Closed on the factory path.** `parseAllowedScopeMarker` → `commitWorkerWorkspaceChanges`. Residual: cli-agent-adapter still no scope (named in the 0610 postcard). |
| I4 MEDIUM floor | **Closed.** `shapeSizeFloor` from shaper units; SMALL + ≥2 required seam groups is refused. |
| B5 post-commit lie | **Closed.** Existing commit returns sha WITH refused extras. |
| I5 two-unit postcard | **Open, honestly.** 0610: a two-area story still produced one unit and a SMITH. Not faked. |
| I7 spend source | **Not attacked this read.** Prior fact stands until you show a query where NULL/widgets drop. |
| I8 source-grep | Assume dropped with I6; I did not re-grep the test file tonight. |

## What I believe from the new work (not the story count)

- Deploy with no producer HOLDs and records no deployment (`72706ad8`). Right law.
- Manifest tool refuses to overwrite a file it does not recognise. Release row names the first broken step, not the last. Eligible receipt exists (`6866de5c`, `d555058`).
- Claim clock no longer parses live claims as stale (`+00` + `Z` → NaN).
- QA disposition is one vocabulary; a losing worker writes no disposition; a verdict names its commands and the revision it was made by.
- Acceptance mapping has a producer. Re-run retires the superseded task. Completed-task release is a no-op so the wave can settle siblings.
- Test budget + section split without moving fences. Protected-file gate. Column-writer audit lives *outside* `manifest/`.

That is the factory getting harder to lie to, which is the 95.

## Still open, in order

1. **I5 intake.** Cut one story whose required findings are two independent seam groups. Cap 2. Postcard with two `smith_split_work` lines in one wave, or no postcard. Do not raise the cap.
2. **cli-agent-adapter `allowedScope`.** Same list as factory. Named residual.
3. **Spend coverage.** Every run a closed source; widgets out of the dollars column.
4. **Worktrees stay unrestored.** Packet forbade sneaking them into the wave. Keep the forbid until a story is only about that.

Sprint leftovers and the remaining ~30 are product/forge stories. Run them as stories. Do not open PARALLEL-WAVE-02.

## Stop

95 is not a missing feature list. It is I5 unobserved plus two named residuals. Go cut the two-unit story or park and sleep.

## Reply

_If you write: I5 fired or I5 still one unit. File:line on cli-adapter if you close it._
