{
  "note": "Debt recorded when pnpm forge:packet-lint was introduced (2026-09-15, FORGE-GATES-01). Each key is `path::rule`; a finding listed here is reported as a warning instead of failing the lint, so a NEW violation blocks while pre-existing debt does not disable the gate on day one. Remove keys as the debt is paid; never add a key to silence a violation you just introduced.",
  "debt": "EMPTY as of 2026-09-15, the same day it was recorded - both original debts were PAID rather than carried. (1) Four V4 packets used `## Skills` as a free-text capability list (16 'skills' in one of them) instead of naming packs from KNOWN_SKILLS; each now names `workflow`, the pack that actually applies to engine work, and the capability prose that mattered is gone rather than left to look loadable. (2) Three citations from an older file layout were stale: `engine.ts` and `db/document-form-instance.ts` no longer exist anywhere, and `components/portal/projects-workspace.tsx` had shrunk below the cited 889-908. Each is now re-pointed at the file that holds the code today (`workflow_engine/lib/workflow/engine.ts:1966-1975`, `lib/forms/form-instance-io.ts:121`/`:102`, `components/portal/projects-workspace.tsx:760-784`). The list is kept rather than deleted because the gate reads this file, and 'no debt' should be a stated fact rather than an absent file. Worth keeping: the rule that found the citations was calibrated before it was trusted - its first draft fired 16 times and 15 of those were the packets' bare-filename shorthand, so the basename resolver replaced it.",
  "findings": []
}
  "debt": "Two separate debts, both in packets this story does not touch. (1) Four V4 packets use `## Skills` as a free-text capability list (16 'skills' in one of them) rather than naming packs from KNOWN_SKILLS, while the packet format (docs/agent/packets/README.md) says it lists packs. (2) Three citations from before the current file layout are stale: `engine.ts`, and `db/document-form-instance.ts`, no longer exist anywhere in the tree, and `components/portal/projects-workspace.tsx` has shrunk to 877 lines while PROJECTS-WORKSPACE-12 still cites 889-908. Found by rule 10 on its first live run (2026-09-15, PIRATE-01); the rule was calibrated first - its first draft fired 16 times, and 15 of those were the packets' bare-filename shorthand, so a basename resolver now handles that instead of a stricter rule that people would switch off.",
  "findings": [
    "docs/agent/packets/ENG-FORGE-V4-10.md::skills-unknown",
    "docs/agent/packets/ENG-FORGE-V4-10.md::skills-too-many",
    "docs/agent/packets/ENG-FORGE-V4-10B.md::skills-unknown",
    "docs/agent/packets/ENG-FORGE-V4-10B.md::skills-too-many",
    "docs/agent/packets/ENG-FORGE-V4-10C.md::skills-unknown",
    "docs/agent/packets/ENG-FORGE-V4-10C.md::skills-too-many",
    "docs/agent/packets/ENG-FORGE-V4-11.md::skills-unknown",
    "docs/agent/packets/ENG-FORGE-V4-11.md::skills-too-many"
  ]
}
