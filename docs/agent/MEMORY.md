# Decision log

Short facts that are expensive to rediscover. Not the current story — that is the packet.

- WhatsApp is an interaction *channel*, not a person-identity type. Actors resolve through strict E.164 `phone`.
- Story Board + `agent_work_item` is the only coding queue. Ready is authorization.
- One Claimed/Running item system-wide (for now). One active item per story always.
- Lanes are job shapes (Scout → Architect → Lead → Smith 1..N → QA → DEV_OPS), not department experts.
- QA contains Candidate Assay as an operation: exact-SHA PASS/FAIL math, prose never overrides arithmetic.
- Inspector is a QA independent-review capability (lineage separation), not a seventh agent. Archive is a capability; night is a Smith grade.
- Six Forge roles: Scout (what is going on?) → Architect (what should we build/change?) → Lead (how do we get this story done?) → Smith (can I build it correctly?) → QA (is it correct and ready to ship?) → DEV_OPS (can I safely get it into production and prove it?).
- Spend vision: team map owns model selection; forge-native runs pin exact models via dsh --patch; model_used persists per run (107); Lead PRE decides grades against relative prices.
- Skills are markdown packs. Neon skill packs are knowledge. Neon Functions are not Smith.
- Architect and Inspector are judgment-lab labels. They auto-run overnight so Smith can produce code. Humans review a working SHA in the morning pack, they do not sit on an empty Architect gate.
- Assay is TUNIT with an allow-list. Empty assay list means Assay does not launch.
- Only `builder` may keep a git commit or write DEV. Scout/Assay commits are rewinded.
- `main` is production-sensitive. Agents commit locally and do not push.
- Code + DEV schema + PROD schema + verification = done for schema stories.
- Do not reset PROD or copy DEV over PROD to fix drift.
- Google Maps is the production map. Never use the Demo key in production.
- Errors are captured durably, never silently: DB (gateway) + service kernel (`ServiceErrorSink` via `composeCoreServices`/`appServiceErrorSink`) + route/action seams (`withApiHandler`/`withServerErrorCapture`/`captureServerError`). Expected business outcomes (validation, FORBIDDEN, not-found) are audited control flow, never error rows. See AGENTS.md "Error Capture Obligation". New code that fails without the framework is a review reject.
- FORGE_ROUTING_BRAIN defaults to engine (ENGINE is the one canonical brain; the legacy reducer runs only when explicitly set via FORGE_ROUTING_BRAIN=reducer for tests/migration). engine mode drives FORGE_SDLC and skips hydrate/follow/publish. Never dual-write.
- ENG-FORGE-V5-21 (captured for the NEXT project, do not build this cycle): Cline beat Forge on real tasks because Cline keeps ONE continuous working context while Forge gave each lane amnesia (Scout→Architect→Lead→Smith→Repair each got a fresh model session reconstructing the problem). Fix = STORY-RESIDENT OpenCode session continuity: resume the SAME story-bound OpenCode session across nodes so context persists, but Forge reasserts role/authority per lane — "context persists; authority does not." Candidate freeze is a HARD CONTEXT WALL: Reviewer/QA must be a fresh independent context, and Deterministic Assay stays the final authority. Open risks to prove before trusting: resumed-session role permission retention; model-switch-on-resume bugs; Forge must persist the opaque OpenCode session/task id; NO session reuse across stories/worktrees; candidate freeze terminates the shared construction context. Tooling helps agents operate better but does NOT fix this underlying cognitive handicap.
- Cost/forecast units are Forge WIDGETS (model weight × minutes, normalized, model-relative), NOT USD and NOT tokens. Tokens = provider metering. USD is reserved for actual settlement only. A future USD/widget spot-rate conversion layers on later (per-model rate × tokens × $). Widgets make the model a replaceable component: swap provider/adapter + rate row, never the workflow/gates/actuals. Schema: `storyboard_story_run.cost_widgets` (+`cost_source`) and `work_estimate.estimated_widgets`; `cost_usd` stays null unless real vendor spend.
- ENG-FORGE-PHASE-AGENT (IMPLEMENTED + live-validated — see docs/agent/packets/ENG-FORGE-PHASE-AGENT.md): abstract `ForgePhaseAgent` owns the role/phase lifecycle: write-on-exit `storyDeliverable` (Scout→`context_refs`, Architect→`architect_brief`), `missingDeliverables` per-flavor gate, `routingDecisionMissing` (research_disposition / lead_decision must be present+valid — a persisted plan is NOT enough to route), bounded self-heal reprompt (`buildSelfHealDirective`, `FORGE_DELIVERABLE_RETRIES` default 1), and enforcement **ON by default** (`deliverableEnforcementEnabled`; disable with `FORGE_ENFORCE_DELIVERABLES=0`). Built in `704e177`→`8892b6c`→`ac31d11`→`847aa21`. Live proof SIMPLE-RUN-03: Architect #1 missed disposition → self-heal → Architect #2 `IMPLEMENT` → advanced; Lead valid `HOLD`. Hosts V5-21 context continuity (still parked).
- Backlog review 2026-09-09: **knip config now committed** (`knip.json`, deterministic `pnpm knip`). **forge_workflow_evidence serializer round-trip assay added** (DB-backed, DEV). **V5-21 still relevant** — enforcement made each lane *deliver* but did NOT fix lane amnesia (each phase still rebuilds context fresh); do not build until Phase 0 qualifies installed `opencode` (v1.18.28 present): prove same-session resume across Flash↔Pro + real permission recompute READ→WRITE→READ + restart-recovery, else HOLD. **RTK / Serena** still "not wired" into the runtime adapter — wiring them is a real harness change needing its own bounded dogfood proof; do not half-wire.
- **2026-09-09 watch (HELM):** V5-21 session continuity **PROVEN** — same OpenCode session reused across a real Forge role chain (marker→`--continue`, log-correlated) AND across Flash↔Pro↔Flash. Env-gated `FORGE_SESSION_CONTINUITY=1`; adapter wired (`4c97867`). Dispatch/anti-token-fire policy toolkit built in TS (Praxis/claude-orchestrate concepts, no system imports): `forge-dispatchability`, `forge-difficulty-scorer` (p_success logistic + learned-swap seam), `forge-execution-shaping` (≤3-chunk plan + validator), `forge-failure-triage` (taxonomy + 3-dispatch budget + 2 retry shapes), `forge-context-lessons` (injected loop), `forge-plan-difficulty`, `forge-dispatch-gate` (`assessSmithDispatch` → GO/FLAG/HOLD). Real Lead eval passed Course-6 (SMITH + 3 serial chunks + merge gate). FINAL-02 full-chain WRITE: Smith produced a real candidate; **caught+fixed 3 real bugs** (lead_post routing ×2 `2b25b4f`/`0935ddc`; QA verification-gap deadlock → `verificationGap` seam `cb8849e` + evidence detection `a89b1f3`, verified live). **Open:** (1) dispatch-gate not authoritative pre-Smith; (2) thresholds not learned from 30–50 runs; (3) restart-recovery of the session binding; (4) deterministic-assay-adapter unit test `deterministic Assay executes frozen Story Run commands` FAILS under the plain unit runner (expected Complete got Hold) — looks pre-existing (fake context uses `executionEnvironment: 'LOCAL'` which the execution-target safety tightened in `f63d6da` now rejects); NOT caused by the HELM watch commits; RESOLVED later the same date — root cause was the post-Assay dependency-cruiser static gate failing in a bare temp workspace (no package.json → `ERR_PNPM_RECURSIVE_EXEC_NO_PACKAGE` → policy violation → Hold); `runStaticGate` now skips depcruise when no package manifest exists (prod workspaces always carry package.json), adapter test green 2/2.
- DEV hygiene: run `APP_ENV=development node --env-file=.env.local scripts/forge-cleanup-dev.ts --apply` (dry-run default) to prune orphaned engine rows + stale worktrees so accumulated garbage never interferes with real runs.
- **2026-09-09 (dispatch-gate landing, `28af2ce`):** the KRAKEN gate now has a RUNNING enforcement seat, not just a library. `forge-dispatch-seam.ts` (`assessSmithWork`) is the single adjudication the Smith anti-token-fire path in `agent-runtime-role-runner` calls (behavior-preserving: OVERSIZED/>3 chunks HOLD; absent/malformed/in-bounds GO) and it UPGRADES to the full `assessSmithDispatch` when a structured plan is supplied. This closes open-item (1)'s *running-seat* half. **Next expedition (fresh branch `forge/lead-structured-plan`):** the fully pre-Smith gate still needs LEAD to emit a structured plan (Lead currently emits prose chunk-lines only, no machine plan) — a prompt-contract + parser change so the gate can block BEFORE Smith spends tokens. See FORGE-WORKSHOP.
- **2026-09-09 (release to origin/main + PROD):** pushed local `main` (`2f71c66..5c5a47c`, 36 commits incl. Projects real wiring + the P0/P1/P2 Forge review work) to origin/main. Applied migrations to PROD control plane: **124** (`wbs_project`+`wbs_item`, was missing) then **140** (repoint `wbs_item.project_id` FK → `project(id)`). Verified PROD via `pg_class`: FK `wbs_item_project_id_fkey` → `project`, tables `project/wbs_item/wbs_project` present, `storyboard_story.forge_v1_legacy` (139) present. `.env.local` de-duplicated (single `DATABASE_URL`/SANITY keys; generic URL = DEV, PROD URL separate). **Corrected finding:** PROD ALREADY has `mv_client_directory` (MATVIEW) + `person`/`person_identity`/`property` — so `/portal/projects` renders safely in PROD; an earlier `information_schema.tables` presence check was unreliable (reported objects as absent). `contract` is genuinely NOT in PROD (unreleased domain feature); `/portal/projects` does not read it to render, so it is left OFF PROD until the feature is solid (captain: PROD data is ODS-owned; never migrate DEV client/property data to PROD; schema-only is fine; data populate needs a conversation). An attempted re-run of `080_mv_client_read_models.sql` failed (`CreateTableAsRelExists`) because PROD already applied an earlier version — rolled back cleanly, no PROD change; do NOT re-run non-idempotent mv migrations on PROD.

- **2026-09-09 (QA must PASS and QUIT - never hang/recall Smith):** hardened `runAssayCommand` (deterministic-assay-adapter). Root cause of the QA hang: the assay child was spawned with `shell:true` and a timeout killed ONLY the shell - a shell child (the real test process) could keep the pipes open so `close` never fired and QA hung indefinitely past the (15-min) timeout. Fix: spawn `detached` (own process group); on timeout SIGTERM the whole tree, then SIGKILL + force a terminal `timedOut` result after a 5s grace (absolute bail). A command can now never leave QA hanging - worst case it returns `timedOut` -> Assay FAIL -> Hold at timeout+5s, and on a real pass it still terminalizes PASS and quits (verified: deterministic-assay-adapter.test 3/3, incl. new "stalled command times out and terminalizes" regression test; tsc clean).

- **2026-09-10 (run usage capture gap + opencode discovery):** story runs have `tokens_input`/`tokens_output`/`cost_usd`/`cost_widgets` but NOTHING writes tokens (0 across 204 PROD runs; 52 widget rows) — so calibration/regression has no spend signal. ROOT: the harness adapters (opencode `opencode run`, deepseek `dsh`) capture only stdout/exit/elapsed and never read usage. DISCOVERY: OpenCode DOES expose usage — CLI `opencode export <sessionID>` / `opencode stats [--days N] [--models N] [--project]`, and locally in `~/.local/share/opencode/opencode.db` the `session` table stores `cost`, `tokens_input`, `tokens_output`, `tokens_reasoning`, `tokens_cache_read`, `tokens_cache_write`, plus `directory` (worktree) + `model`. Plan packet: `docs/agent/packets/ENG-FORGE-RUN-USAGE.md` (capture in the harness adapter -> thread -> always-write persist in a finally -> tests). Never write estimated/heuristic tokens; null is honest, a fake number poisons calibration. DeepSeek `dsh` usage path still unknown (bin not on PATH here).

- **2026-09-10 (two-way schema drift found + DEV→PROD promotion + parity tool):** Discovered DEV and PROD had been diverged for weeks in BOTH directions. DEV was ahead on business schema: migrations **116,117,118,119,120,121,122,138** never reached PROD (`contract*`, `firm*`, `person_firm/person_person`, `role_alias`, `security_role`, `regrid_culebra_parcel`, property `address_line1/finca_number/registry_entry/registry_section`, transaction/document-form `contract_id`, showing report cols). PROD was ahead on Forge dispatch: `agent_work_item.{lane,run_phase,player_id,provider_id,model_id,harness_id,field_id,parallel_group_id,parallel_size,parallel_slot,split_assignment,candidate_shas}` + `storyboard_story_run.lead_split_assignments` exist in **no migration** (out-of-band) — that is why `db/agent-work.ts` needs its schema-agnostic upsert. ROOT CAUSE: no migration ledger (`apply-migration.mjs` executes a file and records nothing), so nothing could detect drift. SYMPTOM (symmetric production/dev breakage): DEV `system-health` + `settings-auth` errored `42703 role.account_type does not exist` and every `app_user_role` join silently matched business-position rows (empty result = dev auth downgrade); PROD `auth-status` errored `42P01 relation "security_role" does not exist`. FIX SHIPPED: code `8584138` — all authorization lookups (`app_user_role`/`role_authority`/`account_type`) now read **`security_role`**; business-position joins unchanged (`role` / `role.scope`, e.g. `contract-service-repository`, `pns-canonical-binding`). PROD PROMOTION APPLIED: `116,117,118,119,120,121,122,138` in order (118 renames `role`→`security_role`, `relation_role`→`role`, `relation_role_alias`→`role_alias`). PROD snapshot taken first (`/tmp/prod-promo-backup-2026-09-10T02-13-39-064Z`, 90 tables/103,745 rows + `role` dump). REVERSE DRIFT RECORDED: `142_forge_dispatch_columns.sql` (the 12 `agent_work_item` cols + `lead_split_assignments`, applied to DEV). NEW TOOL: `scripts/check-schema-parity.mjs` (read-only DEV vs PROD, exits non-zero on drift) — **PARITY OK** as of this entry (101 tables each, 0 column drift). SUPERSEDES the earlier "leave `contract` OFF PROD" stance: `contract`/`contract_*` schema now exists on PROD (SCHEMA ONLY — zero rows; no business data was migrated to PROD). OPEN / ASK-FIRST: index + lock semantics intentionally still differ — DEV keeps the global `agent_work_item_single_active` system-wide single-active lock while PROD uses `agent_work_item_one_serial_active_per_story` + `agent_work_item_one_parallel_slot` (parallel/serial model). Relaxing the DEV single-active lock is an explicit human decision (AGENTS "Ask first"), not a side effect of 142.


- **2026-09-10 (DEV adopts PROD dispatch model + PROD→DEV data pull, Projects work preserved):** (a) **Lock model:** `143_forge_dispatch_parallel_lock.sql` dropped DEV's global lock (`agent_work_item_single_active` + `one_active_per_story`) and installed PROD's serial + parallel-slot unique indexes — captain's authorization ("we ran engine PROD tonight and workflow in PROD is better"). DEV/PRD `agent_work_item` indexes are now identical. (b) **Preserved DEV Projects work:** `db/seeds/dev-projects-workspace.sql` (generated by `scripts/export-dev-projects-workspace.mjs`, idempotent `on conflict do nothing`) holds 3 projects, 25 WBS items, 2 `wbs_project`, 1 contract + relations and the DEV-only property/person anchors. (c) **Data pull:** `scripts/pull-prod-to-dev.mjs --apply` copied PROD→DEV — 76 tables / 102,087 rows (person 2890, interaction 50,220, transaction_document 45, document_form_instance 5, media 175). DEV snapshot first: `/tmp/dev-snapshot-2026-09-10T02-32-46-151Z` (101 tables / 101,400 rows). PRESERVED on DEV: auth (8 tables), `project`/`wbs_item`/`wbs_project`, `agent_work_item`, `storyboard_*`, `app_error`, plus FK-closure (`forge_engine_task_execution`, `forge_workflow_evidence`, `forge_hold_record`, `forge_tool_artifact`, `work_estimate`). Verified: every copied table matches PROD, all project anchors present, PARITY OK. (d) **Two hard-won lessons:** hand-rolled SQL literals break on text[]/jsonb — use parameterized inserts (`lit()` produced `'["a","b"]'` → `malformed array literal`); and a preserve closure must NOT cascade from the auth tables because `app_user`/`security_role`/`authority` ids are IDENTICAL across DEV and PROD (so business FKs to them resolve), while `role` ids differ but every role-FK table (`contract_*`, `person_firm`, `person_person`, `firm_property`) is empty in PROD.


- **2026-09-10 (DEV_OPS elevated — the role we under-built):** Captain's correction: **"pull Neon PROD down to DEV" means reset the DEV Neon branch from PROD** (instant, byte-exact, carries sequences/indexes/partitions). The row-by-row `scripts/pull-prod-to-dev.mjs` is the *selective/partial* fallback, not the normal path — use it only when DEV-owned state must be preserved. Critically: **a branch reset hides drift rather than fixing it** — the 2026-09-10 drill found PROD missing 116–122/138, DEV missing the undocumented Forge dispatch columns, and the half-applied 118 rename *only because* we compared schemas instead of re-branching. So the durable pair is: branch for refresh, `pnpm db:parity` for truth. Operating contract is now `docs/agent/DEV-OPS-DATABASE-PLAYBOOK.md` (topology, promotion order, hard-won rules, DEV_OPS checklist) and is referenced from AGENTS.md "Database Delivery Rule". New scripts: `pnpm db:parity`, `pnpm db:pull:dev`, `pnpm db:export:projects`, `pnpm db:seed:projects`. - **2026-09-10 (migration ledger + DEV_OPS gate BUILT):** The two things flagged as missing are now implemented. (1) **Ledger:** `schema_migration` (migration 144, applied dev+prod) records filename + sha256 checksum + target + note; `scripts/apply-migration.mjs` writes it on every apply, *skips* a re-apply whose checksum matches and *refuses* one whose checksum changed; `pnpm db:migrations` reports what is recorded where, flags one-sided records and lists unrecorded (pre-baseline) files. Baseline backfilled honestly: only the migrations actually applied on 2026-09-10, the DEV-only structural ones, and a `<baseline>` row stating that parity was verified — pre-baseline history is never invented. (2) **DEV_OPS gate:** `workflow_app/forge/release-operations.ts` now records to the durable ledger on `applyMigrations`, and `verifyMigrations` fails closed unless each migration is checksum-matched, **present in the ledger**, and **DEV/PROD parity is clean**. Shared implementation: pure comparison in `lib/schema-parity.ts` + DB reader in `workflow_app/forge/schema-parity.ts` (kept out of `db|lib|app` because the ARCH test forbids the Neon driver there — that test caught the first draft, which is exactly its job). Tests: `testv2/schema-parity.test.ts` (5 pure cases). Also fixed a trailing-whitespace invariant violation in `forge-phase-agent.ts:130`.
 The migration ledger that was missing is now built: `schema_migration` (migration 144) records every apply (filename, sha256 checksum, target, timestamp; re-apply skipped on checksum match, refused on mismatch); `pnpm db:migrations` reports what is recorded where and flags one-sided records. Ledger is authoritative from the 2026-09-10 baseline forward; pre-baseline entries are explicitly marked rather than invented.



- **2026-09-10 (review items #1/#2 closed — workshop truth + the pre-Smith assertion):** (1) `docs/agent/FORGE-WORKSHOP.md` live snapshot refreshed from the 2026-09-09 "morning" version to 2026-09-10 truth: engine-default routing; the pre-Smith fuse is machine-enforced (`assessLeadHandoff` at `lead_pre`, missing/malformed `LEAD_PLAN` on SMITH/SPLIT = HOLD, no Smith adapter starts); difficulty FLAG-only; Ready-gate `missing-assay-plan`; QA always terminates (process-group kill); classifier routes on `failureClass`; MINIMAL DEV_OPS completes at publish (CI owns prod smoke); the new schema gate (`db:parity` + `db:migrations` + `verifyMigrations` fail-closed); V14 proven to `completed`; and the honest gaps (usage still zero, session continuity env-gated, no human-free publish yet). The two stale bullets ("DO NOT ASSUME" pre-Smith use; "NOT YET ESTABLISHED" full chain) are gone. (2) The review's asked-for assertion now exists and is real rather than structural: the runner's decision was extracted to a pure `assessLeadHandoff(nodeId, leadDecision, notes)` in `forge-lead-plan.ts` (behaviour-preserving swap at the `lead_pre` call site) so tests assert the RUNNING path's verdict — missing or malformed plan on SMITH/SPLIT HOLDS, a valid plan GOs, SOLO/HOLD are not gated, and the gate never applies on a `smith` node. Also corrected the stale `forge-dispatch-seam.ts` header: the full KRAKEN gate is no longer "test-only" — it runs live pre-Smith; the seam remains the post-Smith envelope guard. Suites: app 1822/1825 (0 fail), engine 48/48, forge engine 273/0, tsc clean.

- **2026-09-10 (the 6 red tests: 1 real production bug + 5 stale specs):** Triaging the pre-existing `test:app` failures found a genuine production defect. **BUG:** `lib/agreements/crm26-consumer.ts` completes the Contract workflow by looking tasks up as `select id from tasks where process_instance_id = $1 and node_id = $2 …`, but the engine never wrote `node_id` (it wrote the node's display `name`) and **the column did not exist** — verified live as `42703 column "node_id" does not exist`. So contract execution could never complete `pns_executed` and the workflow never advanced past `pns_preparation`. FIX: `db/migrations/145_tasks_node_identity.sql` adds `tasks.node_id` (+ index on `(process_instance_id, node_id, status)`); `workflow_engine/lib/workflow/engine.ts` now writes `${node.id}` on task creation; `testv2/engine_tests/fake-sql.ts` stores `node_id` **and honours the `node_id` filter** (it previously returned any actionable task, hiding the bug). Applied to DEV **and** PROD and recorded in the `schema_migration` ledger — first migration promoted under the new gate. The other five were stale specs, each lagging an intentional change: `agent-work` fake param mapping missing `test_mode/assay_commands/packet_sha` (index shift made `actual_start_at` read the wrong param); DOC-06 proof 2 (`OFFER-01` now owns a deliberate `brokerName` default, comment says why) and proof 10 (`de4581a` "make required-field validation advisory" — `validateFormValues` returns `[]` by design; the proof now asserts an issued PDF is still well-formed and nothing invents values); FORM-SERVICE-CUT-01 + DOC-08 exposed a **contradictory binding contract**: `TemplateFieldDefinition.binding` is declared OPTIONAL (`binding?: TemplateFieldBinding`), so an unbound field must OMIT the key rather than carry `null` — `lib/forms/xml-template.ts` fixed to the repo's spread-omit convention, DOC-08 specs aligned to `undefined`; SUPPORT-2 test 11 lagged the Clients refactor (`ClientManager` → `ClientLens` in `components/portal/clients-workspace.tsx`, rendered via a `ClientsWorkspace` alias). Lesson: a red test is a claim about intent — check `git log` for the intent before "fixing" the code to satisfy it.



- **2026-09-10 (review item #4 DONE — first fully quiet end-to-end completion):** After the Lead-SOLO authority fix (`7cfc76c`), re-ran `ENG-PROJECTS-ANCHOR-01` headless. Result: `"status":"completed"`, `"needsHuman":false`, `"exhausted":false`, steps `architect → lead_pre → lead_solo_implement → lead_post → qa_verify` — **no human anywhere on the last mile**. Evidence: `lead_decision=SOLO` (honoured, no SPLIT), `candidate_sha=qa_verified_sha=published_sha=1952061…`, `qa_passed=true`, `publish_succeeded=true`, story `Complete`/100. Publish pushed the candidate; local `main` fast-forwarded to `1952061`. Post-publish verification on main: scoped assay 9/9, `tsc` clean, app suite 1825/1828 (0 fail). WHAT SHIPPED: `ui/projects/service-projection.ts` now derives per-project **effective anchors** (project row value wins, else the distinct person/property entity anchors from that project's WBS items) for the Pane 2 documents/activity projections (+ its 4 tests). NOTE on the visible result: the DEV panes still show 0 because the data does not link — 42 documents hang off property `…-0003` while the WBS anchors are `pr1`, `sea-to-soul`, `…-0001`, and every `project.property_id` is null or synthetic. That is a data/anchoring matter, not a code defect; the mechanism is now correct. STILL OPEN (recorded separately): the model-pin mismatch (team map pins Lead to `lead-pro`/deepseek-pro + `forge-native`, but all roles actually ran `opencode` + `deepseek/deepseek-v4-flash`) and the broken SPLIT lane (branch name mangled, child worktree never moved, candidate capture read the wrong workspace). Item #3 `ENG-FORGE-RUN-USAGE` is next.

- **2026-09-10 (ENG-PROJECTS-ANCHOR-01 quiet-run attempt: found a real control-plane routing bug, no completion yet):** Ran a tiny real story end-to-end headless (`pnpm forge:engine --story ENG-PROJECTS-ANCHOR-01 --work-type FEATURE`; packet + seed committed BEFORE the run per the cadence rule). Chain: architect done → lead_pre done → **`smith_split_work`** → HOLD after 2 attempts: "role did not deliver smith-candidate". **The work was actually DONE.** Smith committed a real candidate `a12edf6` ("fix(projects): fall back to WBS entity anchors for pane 2 documents/activities", exactly the 2 scoped files, +79/−11, 4 new tests) and independently verified 9/9 in its worktree. ROOT CAUSE (two layers): (1) **Lead emitted `LEAD_DECISION: SOLO`** with a SMALL 1-chunk `LEAD_PLAN` ("SOLO avoids a pointless Smith handoff and spend"), but `applyLeadShape` in `workflow_app/forge/agents/forge-phase-agent.ts` **overrides Lead's choice with the authoritative shape derived from Architect findings** — and because `leadPlanShape`'s authoritative vocabulary is only SMITH/SPLIT/HOLD, a Lead SOLO with any required findings ALWAYS fails validation and is rewritten to SPLIT. The XML `execution_shape` decision routes purely on the `leadDecision` fact, so the engine went to `split_dispatch`. (2) **The SPLIT lane then lost the candidate**: the child worktree was `…-e0-split-1` (branch name mangled to a trailing `-`) and never moved (HEAD == baseCommit `8be3b51`), while the commit landed in the **base** worktree `…-e0` (HEAD `a12edf6`); the evidence path (`cli-agent-adapter.resultExternal`) compares `git rev-parse HEAD` in the *assigned* workspace against `baseCommit`, so every run recorded `commit_hash = null` → `evidence.candidateSha` null → `smith-candidate` missing → HOLD. So a HOLD occurred AFTER the work was finished — the control-plane-bug class the 86/100 review names, not a Smith failure. Also note the `<dynamic-fork split_dispatch minimum="2">` ran with a single bogus child. OPEN DECISION (needs the captain, do not silently change authority): either (a) honour an explicit Lead SOLO carrying a valid bounded `LEAD_PLAN` (keep the anti-pattern refusal for SMITH-over-multiple-seams) so the run takes `lead_solo_implement` → `lead_post` → QA → publish; or (b) keep the override but fail CLOSED (HOLD at lead_pre with a named reason) instead of silently routing into the unproven SPLIT lane; or (c) fix the SPLIT lane's workspace/branch naming + candidate capture first. Until one lands, a Lead SOLO over multiple Architect findings cannot complete.


- **2026-09-10 (Astra LEAD routing INTEGRATED + first live validation):** Landed the handoff (`docs/agent/CULEBRALUXE-LEAD-ROUTING-CODE.md`) — the tested decision boundary is now on the hot path. Files: `workflow_app/forge/forge-lead-routing.ts` + `-prompt.ts` + tests (25/25 in-repo), and a new trusted-hydration module `workflow_app/forge/lead-routing-context.ts` (`buildLeadRoutingContext`, `findLatestAcceptedLeadRouting`). Wiring: `agent-runtime/enqueue-lane.ts` accepts a trusted `leadRoutingContext` and injects the single `LEAD_ROUTING` directive for lead/pre; `agent-runtime/lead-decision.ts` PRE text de-contradicted (no more "SPLIT:n is DISABLED" vs "→ SPLIT:n"); the runner now reviews the raw LEAD PRE output (`reviewLeadProposal(parseLeadRouting(raw), context)`) and pushes errors into the EXISTING bounded self-heal `miss` path **even when `FORGE_ENFORCE_DELIVERABLES=0`**, then assigns `leadRoutingFacts` to evidence only on acceptance. RETIRED from the hot path: `applyLeadShape` (deleted — the silent shape override that rewrote Lead's SOLO), the PRE `assessLeadHandoff` call, and the `lead-plan:` persistence; the accepted assignment plan is now recovered by RE-VALIDATING Lead's own `LEAD_ROUTING` run note (`findLatestAcceptedLeadRouting`), and work orders are injected into the serial Smith lane **and** `lead_solo_implement` (previously Smith-only). Runtime capability is declared honestly: `splitEnabled: false`, `maxSmiths: 1` — a Lead SPLIT proposal is CORRECTED rather than routed into the unproven split lane, which makes the whole durable-`splitPlan` work (handoff items 6–8) inert until SPLIT is fixed. Verification: tsc clean; test:app 1847/1850 (0 fail); test:engine 48/48; forge engine 298/0. FIRST LIVE VALIDATION (`ENG-PROJECTS-ANCHOR-02`, reset+run): the mechanism behaved exactly as designed — Lead proposed, code REJECTED with a specific actionable reason (`lead-routing:a: qualitative: out-of-range risk: worker-fit (worker would be set up to fail)`), reprompted once, then HELD. Root of that reason: the KRAKEN gate's hard bound is `REDFLAG_HIGH = 4` (`forge-dispatchability.ts`), so any risk dimension rated 5 is NOT_DISPATCHABLE; Lead rated `workerFit: 5`. Contrast with the EARLIER HOLD the same night: that one was a control-plane bug (work finished, decision silently overridden); this one is the control plane WORKING — propose → refuse with a named reason → stop instead of burning a worker. OPEN: the directive tells the model risks are 1..5 but not that **5 = non-dispatchable**; teaching that would let Lead either correct a mistaken 5 or HOLD deliberately (candidate follow-up, do not silently edit the handoff prompt without the captain).

- **2026-09-10 (Astra routing refinement PROVEN end to end — closes the "5 = non-dispatchable" open item above):** the captain's ruling landed: **Lead's self-rated risk vector is RECORDED EVIDENCE, never a dispatch veto.** `reviewLeadProposal` now runs the captain's `dispatchabilityFor(features)` math and records its verdict as an **advisory**, while only the STRUCTURAL gate (`assessSmithDispatch(plan)` — countable plan facts: shape, chunk ceiling) can HOLD. Rationale: the vector is an uncalibrated self-assessment (no run history, no telemetry), so letting it veto lets a guess block the model and re-arms the difficulty veto already made FLAG-only (`9fe43fd`). **Live proof on `ENG-PROJECTS-ANCHOR-02`:** the exact input that HOLDed the previous night (`workerFit: 5`) now yields `lead-routing verdict=GO errors=none advisories=a: self-rated NOT_DISPATCHABLE: out-of-range risk: worker-fit (worker would be set up to fail) — advisory only, not a dispatch veto`, and the chain ran `architect → lead_pre (GO/SMITH) → smith (candidate 0465126) → lead_post (same SHA) → qa_verify (qaPassed:true)`. The dissent is preserved as evidence instead of blocking. Also fixed in the same swing: (a) **one routing contract per lane** — the legacy `lead_pre` `evidenceInstruction` (`FORGE_EVIDENCE_JSON.leadDecision/splitCount` + `LEAD_PLAN`) was still injected alongside the new directive, so the model obeyed the longer legacy text and emitted no `LEAD_ROUTING` line at all (a HOLD with a *blind* reprompt); it is now suppressed whenever a routing context governs PRE, and the self-heal restates the correct contract; (b) `reviewLeadProposal(null, …)` now returns **"No LEAD_ROUTING line was emitted …"** instead of a generic `Malformed`, so a self-heal can act on the actual miss. Advisory/structural split has tests (27/27 in `forge-lead-routing.test.ts`).
- **2026-09-10 (ENGINE BLOCKER — the deploy gate is unsatisfiable; why quiet completions are rare):** `AgentRunEvidence.releaseEvidence` is *consumed* by the `deploy`/`production_smoke` adjudication in `forge-role-mapping.ts` (requires `kind:'deployment'|'production_verification'`, `success`, non-empty `receiptId`, `artifactSha === publishedSha`) but **nothing in the repo ever populates it** — `ForgeReleaseOperations` implements only `applyMigrations`/`verifyMigrations`/`refreshDerived`/`verifyDerived`; there is no publish/deploy/verify-production operation at all, and `deploymentReceipt` is not even in `allowedEvidenceKeys` (so a model cannot legitimately set it). Consequence: any story whose architect declares `deploymentRequired: true` **cannot complete** — it HOLDs at `deploy` after its attempts (`Forge deploy HOLD (after 2 attempt(s)): role did not deliver devops-receipt`). `ENG-PROJECTS-ANCHOR-01` was quiet only because its architect declared no deployment obligation (its chain had no `deploy` node). NEXT STORY (own packet, do NOT fake a receipt — "never report an operation successful without machine evidence"): wire real publish/deploy/verify operations that emit genuine `releaseEvidence` receipts, or make the obligation conditional so a correct run is not blocked by an unimplemented stage. Contributing clarity gap: `repair_devops`/`deploy`/`production_smoke` return `{ lane: 'dev_ops' }` with **no `evidenceInstruction`**, so the role is never told what machine marker it must produce.
- **2026-09-10 (two real DB bugs found by running the pipeline, fixed + promoted):** (1) **Migration 146** — migration 143 widened the serial-lock index predicate to `state in ('Ready','Claimed','Running','Paused') and parallel_group_id is null` but the 025 trigger `agent_work_item_dispatch()` still declared `on conflict (story_id) where state in ('Ready','Claimed','Running')`; Postgres requires the arbiter clause to IMPLY the index predicate, and that clause never constrained `parallel_group_id`, so inference failed with `42P10` and **EVERY insert/transition of a `storyboard_story` to `Ready` aborted — in DEV and in PROD** (143 was live on PROD, so this broke production story dispatch). 146 restates the arbiter as `state in ('Ready','Claimed','Running','Paused') and parallel_group_id is null`; applied + verified on BOTH (a real `Ready` transition now dispatches a serial `agent_work_item`). Migrations **142/143 were also back-recorded on PROD** (their effect was already present, the ledger entry was missing): DEV and PROD now report the same 14 recorded. (2) `leadRoutingFacts` carries the in-memory sentinel `splitCount: 0` for SOLO/SMITH, but `forge_workflow_evidence_split_count_check` permits only `NULL or 2..8` → a non-SPLIT Lead completion died with `23514` on a real run. Normalized at the **repository boundary** (`db/forge-workflow-evidence.ts`: non-SPLIT writes NULL; an explicit `leadDecision` write is now authoritative over `coalesce`, so a former SPLIT count is cleared rather than lingering beside a SOLO decision), with a DB-backed regression test in `forge-evidence-roundtrip.test.ts`.

